import{createHash}from'node:crypto';import{simulateRealtimePaperExecution}from'../../trading/realTimeSimulatedExecutionCoordinator.js';import{DEFAULT_PAPER_EXIT_CONFIG}from'./paperExitConfig.js'
export const PAPER_EXIT_VERSION='paper-exit-v1';const positive=v=>Number.isFinite(Number(v))&&Number(v)>0;const hash=v=>createHash('sha256').update(JSON.stringify(v)).digest('hex')
export function createPaperExitFingerprint({positionId,quantity,quote={},side}={}){const reference=Number(quote.price??quote.last),observedAt=quote.updatedAt??quote.timestamp;return hash([positionId,Number(quantity),observedAt,reference,side])}
function rejected(position,status,blockers=[]){return{positionId:position?.positionId??null,symbol:position?.symbol??null,status,blockers,paperTradingOnly:true,automaticExecution:false,liveOrders:false,brokerExecution:false,engineVersion:PAPER_EXIT_VERSION}}
export function simulatePaperPositionExit({position,account={},quantity,quote={},existingExits=[],paperModeEnabled=true}={},options={}){const c=options.config??DEFAULT_PAPER_EXIT_CONFIG,now=options.now??new Date().toISOString();if(!paperModeEnabled)return rejected(position,'REJECTED',['Paper mode is disabled']);if(!position?.positionId||!position.symbol||!positive(position.quantity)||!positive(position.averagePrice)||!['long','short'].includes(position.side))return rejected(position,'INSUFFICIENT_EXIT_CONTEXT',['Valid position, side, quantity, and cost basis are required']);const requested=Number(quantity);if(!positive(requested))return rejected(position,'REJECTED',['Exit quantity must be greater than zero']);if(requested>Number(position.quantity))return rejected(position,'REJECTED',['Exit quantity exceeds current paper position']);const reference=Number(quote.price??quote.last),observedAt=quote.updatedAt??quote.timestamp,age=Date.parse(now)-Date.parse(observedAt);if(!positive(reference))return rejected(position,'INSUFFICIENT_EXIT_CONTEXT',['Reference price is required']);if(!Number.isFinite(age)||age<0||age>c.maxPriceAgeMs)return rejected(position,'STALE',['Reference price is stale or invalid']);const side=position.side==='short'?'cover':'sell',fingerprint=createPaperExitFingerprint({positionId:position.positionId,quantity:requested,quote:{price:reference,updatedAt:observedAt},side});if(existingExits.some(x=>x.fingerprint===fingerprint))return{...rejected(position,'DUPLICATE_SUPPRESSED',['Identical exit request was already applied']),fingerprint};const trade={id:`paper-exit-${fingerprint.slice(0,24)}`,symbol:position.symbol,assetType:position.assetType,side,orderType:'market',quantity:requested,price:reference,paperTrading:true};const prepared={id:trade.id,symbol:trade.symbol,assetType:trade.assetType,preparationStatus:'ready',proposedPaperTrade:trade,guardrailEvaluation:{guardrailApproved:true},tradeGuardrailReference:{eventType:'trade.guardrail.evaluated'},sourceDecisionReference:{id:position.originatingEvaluationId,status:'operator-confirmed'}};const portfolio={id:account.accountId??'paper-portfolio',cash:Number(account.cash??0),equity:Number(account.equity??account.cash??0),realizedPnl:Number(account.realizedPnl??0),positions:[position]};const execution=simulateRealtimePaperExecution({preparedTrades:[prepared],portfolio,quote:{last:reference,bid:reference,ask:reference,high:reference,low:reference,liquidityScore:quote.liquidityScore??75,timestamp:observedAt},paperTrading:true,liveOrders:false,brokerExecution:false},{emitEvent:false,timestamp:now});const item=execution.realtimeSimulatedExecutions[0],accounting=item?.accountingUpdate,journal=item?.journalRecord;if(item?.executionLifecycleStatus!=='simulated'||!['position_reduced','position_closed'].includes(accounting?.status))return{...rejected(position,'REJECTED',[item?.rejectionReason??accounting?.reason??'Simulated exit was rejected']),fingerprint};const remaining=accounting.positions.find(x=>x.symbol===position.symbol&&x.side===position.side)??null;const resultStatus=accounting.status==='position_closed'?'POSITION_CLOSED':'POSITION_REDUCED';return{positionId:position.positionId,candidateId:position.originatingCandidateId,evaluationId:position.originatingEvaluationId,symbol:position.symbol,strategyId:position.strategyId,status:resultStatus,fingerprint,exitPlan:{positionId:position.positionId,symbol:position.symbol,strategyId:position.strategyId,existingSide:position.side,currentQuantity:Number(position.quantity),requestedExitQuantity:requested,remainingQuantity:remaining?.quantity??0,averageCost:Number(position.averagePrice),referencePrice:reference,simulatedExitPrice:item.executionSimulation?.fill?.fillPrice,fees:item.executionSimulation?.fill?.fees,slippageBps:item.executionSimulation?.fill?.slippageBps,realizedPnlDelta:accounting.account.realizedPnlDelta,evidenceTimestamp:observedAt,paperTradingOnly:true,automaticExecution:false},positionSnapshot:remaining?{...position,...remaining,positionId:position.positionId}:null,accountSnapshot:accounting.account,journal:{tradeId:journal?.tradeId,journalStatus:journal?.journalStatus,realizedPnl:journal?.realizedPnl,decisionGate:journal?.decisionGate},closedAt:now,tradeQuality:position.tradeQuality??null,regime:position.regime??null,evaluationStatus:position.evaluationStatus??'APPROVED_FOR_PAPER_REVIEW',paperTradingOnly:true,automaticExecution:false,liveOrders:false,brokerExecution:false,engineVersion:PAPER_EXIT_VERSION}}
