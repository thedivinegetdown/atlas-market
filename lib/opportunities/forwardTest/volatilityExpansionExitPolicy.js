export const VOLATILITY_EXPANSION_EXIT_POLICY_VERSION = 'volatility-expansion-exit-v1.0.0'
export const VOLATILITY_EXPANSION_MAX_HOLDING_SESSIONS = 10
const positive = (value) => Number.isFinite(Number(value)) && Number(value) > 0
const round = (value) => Number(Number(value).toFixed(6))
const stable = (value) => value && typeof value === 'object' ? Object.keys(value).sort().reduce((result, key) => ({ ...result, [key]: stable(value[key]) }), {}) : value
const fingerprint = (value) => Array.from({ length: 8 }, (_, seed) => Array.from(JSON.stringify(stable(value))).reduce((hash, character) => Math.imul(hash ^ (character.charCodeAt(0) + seed), 0x01000193) >>> 0, 0x811c9dc5).toString(16).padStart(8, '0')).join('')
export const VOLATILITY_EXPANSION_EXIT_POLICY_DEFINITION_FINGERPRINT = fingerprint({ version: VOLATILITY_EXPANSION_EXIT_POLICY_VERSION, stop: 'max(prior20_high-atr14,entry_price-(1.5*atr14))', target: 'entry_price+(2.5*r)', maximumHoldingSessions: 10, sameBarAmbiguity: 'stop_first', gapRule: 'adverse_stop_gap_fills_at_open;favorable_target_gap_capped_at_target' })
export function createVolatilityExpansionExitPolicy(input = {}) { const entryPrice = Number(input.entryPrice), prior20High = Number(input.prior20High), atr14 = Number(input.atr14), enteredAt = new Date(input.enteredAt ?? input.evidenceTimestamp); if (input.strategyId !== 'volatility-expansion-v1' || !String(input.strategyVersion ?? '').trim() || ![entryPrice, prior20High, atr14].every(positive) || Number.isNaN(enteredAt.getTime())) throw new Error('complete volatility expansion entry evidence is required'); const initialStop = Math.max(prior20High - atr14, entryPrice - (1.5 * atr14)); if (!(initialStop < entryPrice)) throw new Error('initial stop must be below entry price'); const riskPerUnit = entryPrice - initialStop; const core = { version: VOLATILITY_EXPANSION_EXIT_POLICY_VERSION, definitionFingerprint: VOLATILITY_EXPANSION_EXIT_POLICY_DEFINITION_FINGERPRINT, strategyId: input.strategyId, strategyVersion: input.strategyVersion, positionSide: 'long', enteredAt: enteredAt.toISOString(), prior20High: round(prior20High), atr14: round(atr14), entryPrice: round(entryPrice), initialStop: round(initialStop), profitTarget: round(entryPrice + 2.5 * riskPerUnit), riskPerUnit: round(riskPerUnit), rewardRiskRatio: 2.5, invalidation: 'initial_stop_reached', maximumHoldingSessions: 10, sameBarAmbiguity: 'stop_first', gapRule: 'adverse_stop_gap_fills_at_open;favorable_target_gap_capped_at_target', missingOrStaleEvidence: 'fail_closed_no_policy_exit', manualEmergencyClose: 'allowed_but_non_policy_compliant_and_excluded_from_cohort_minimum', partialExits: false, trailingStops: false, discretionaryChanges: false, deterministic: true, paperTradingOnly: true, liveTradingApproved: false, strategyFingerprint: input.strategyFingerprint ?? null }; return Object.freeze({ ...core, fingerprint: fingerprint(core) }) }
export function evaluateVolatilityExpansionExitPolicy({ policy, bar = {}, sessionsHeld = 0 } = {}) { const expected = fingerprint(Object.fromEntries(Object.entries(policy ?? {}).filter(([key]) => key !== 'fingerprint'))); if (policy?.version !== VOLATILITY_EXPANSION_EXIT_POLICY_VERSION || policy.fingerprint !== expected) return { action: 'BLOCKED', reason: 'exit_policy_invalid', paperTradingOnly: true }; const [open, high, low, close] = [bar.open, bar.high, bar.low, bar.close].map(Number); if (String(bar.freshness ?? '').toUpperCase() !== 'FRESH' || [open, high, low, close].some((value) => !positive(value))) return { action: 'NO_ACTION', reason: 'missing_or_stale_market_evidence', paperTradingOnly: true }; if (open <= policy.initialStop) return { action: 'EXIT_FULL', reason: 'stop_gap', exitPrice: open, conservative: true, paperTradingOnly: true }; if (open >= policy.profitTarget) return { action: 'EXIT_FULL', reason: 'target_gap', exitPrice: policy.profitTarget, conservative: true, paperTradingOnly: true }; if (low <= policy.initialStop) return { action: 'EXIT_FULL', reason: high >= policy.profitTarget ? 'same_bar_stop_target_stop_first' : 'initial_stop', exitPrice: policy.initialStop, conservative: true, paperTradingOnly: true }; if (high >= policy.profitTarget) return { action: 'EXIT_FULL', reason: 'profit_target', exitPrice: policy.profitTarget, conservative: true, paperTradingOnly: true }; if (Number(sessionsHeld) >= 10) return { action: 'EXIT_FULL', reason: 'maximum_holding_period', exitPrice: close, conservative: true, paperTradingOnly: true }; return { action: 'HOLD', reason: 'no_exit_condition_met', paperTradingOnly: true } }