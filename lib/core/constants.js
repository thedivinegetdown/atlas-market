export const TRADING_MODE = Object.freeze({
  PAPER: 'paper',
  LIVE: 'live',
})

export const DEFAULT_TRADING_MODE = TRADING_MODE.PAPER

export const ORDER_SIDES = Object.freeze({
  BUY: 'buy',
  SELL: 'sell',
})

export const ORDER_TYPES = Object.freeze({
  MARKET: 'market',
  LIMIT: 'limit',
})

export const RISK_LEVELS = Object.freeze({
  LOW: 'low',
  MEDIUM: 'medium',
  HIGH: 'high',
})
