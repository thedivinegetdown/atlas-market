import { describe, expect, it } from 'vitest'
import { createVolatilityExpansionExitPolicy, evaluateVolatilityExpansionExitPolicy } from '../lib/opportunities/forwardTest/volatilityExpansionExitPolicy.js'
const policy = () => createVolatilityExpansionExitPolicy({ strategyId: 'volatility-expansion-v1', strategyVersion: '1.0.0', entryPrice: 120, prior20High: 119, atr14: 4, enteredAt: '2026-08-27T00:00:00Z' })
describe('volatility expansion exit policy', () => { it('freezes stop and 2.5R target', () => expect(policy()).toMatchObject({ initialStop: 115, profitTarget: 132.5, rewardRiskRatio: 2.5, maximumHoldingSessions: 10 })); it('uses conservative ambiguity and gap behavior', () => { expect(evaluateVolatilityExpansionExitPolicy({ policy: policy(), bar: { open: 121, high: 133, low: 115, close: 120, freshness: 'FRESH' } }).reason).toBe('same_bar_stop_target_stop_first'); expect(evaluateVolatilityExpansionExitPolicy({ policy: policy(), bar: { open: 114, high: 120, low: 113, close: 115, freshness: 'FRESH' } }).reason).toBe('stop_gap') }); it('fails closed for stale evidence', () => expect(evaluateVolatilityExpansionExitPolicy({ policy: policy(), bar: { freshness: 'STALE' } }).reason).toBe('missing_or_stale_market_evidence')) })