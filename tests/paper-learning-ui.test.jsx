import{act}from'react';import{createRoot}from'react-dom/client';import{afterEach,describe,expect,it}from'vitest';import{PaperLearningPanel}from'../src/workspaces/Reports/reportSections.jsx'
let root,container;function render(e){container=document.createElement('div');document.body.appendChild(container);root=createRoot(container);act(()=>root.render(e));return container}afterEach(()=>{act(()=>root?.unmount());container?.remove()});const evidence={status:'UNDER_REVIEW',sample:{status:'EARLY',completedTrades:10},performanceStatus:'CAUTION',recentTrend:'STABLE',coverage:{percentage:80},qualityCalibration:{status:'MIXED',reason:'not monotonic'},strategyEvidence:[{strategyId:'momentum',evidenceState:'PROMISING',sampleSize:10,performance:{expectancyPerTrade:5}}],regimeEvidence:{trend:[{regime:'BULL',sampleSize:10}],volatility:[],risk:[]},observations:['Quality-score calibration is mixed.'],reviewActions:['REVIEW_QUALITY_CALIBRATION']}
describe('paper learning UI',()=>{it('renders loading empty and insufficient states',()=>{expect(render(<PaperLearningPanel state={{isLoading:true}}/>).querySelector('[role=status]')).toBeTruthy();act(()=>root.unmount());root=createRoot(container);act(()=>root.render(<PaperLearningPanel state={{isLoading:false,evidence:null}}/>));expect(container.textContent).toContain('No realized');act(()=>root.unmount());root=createRoot(container);act(()=>root.render(<PaperLearningPanel state={{isLoading:false,evidence:{...evidence,status:'INSUFFICIENT_EVIDENCE',sample:{status:'INSUFFICIENT_SAMPLE',completedTrades:2}}}}/>));expect(container.textContent).toContain('Sample too small')});it('renders populated calibration strategy and boundaries',()=>{const v=render(<PaperLearningPanel state={{isLoading:false,evidence}}/>);expect(v.textContent).toContain('MIXED');expect(v.textContent).toContain('momentum: PROMISING');expect(v.textContent).toContain('PAPER ONLY');expect(v.textContent).not.toMatch(/enable strategy|change weight|live trade/i)})})
