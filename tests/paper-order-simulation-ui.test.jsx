import {act} from 'react';import{createRoot}from'react-dom/client';import{afterEach,describe,expect,it,vi}from'vitest';import{PaperSimulationPanel}from'../src/workspaces/Scanner/scannerSections.jsx'
let root,container;function render(e){container=document.createElement('div');document.body.appendChild(container);root=createRoot(container);act(()=>root.render(e));return container}afterEach(()=>{act(()=>root?.unmount());container?.remove()})
describe('paper simulation UI',()=>{
 it('shows paper boundary and does not run on mount',()=>{const run=vi.fn();const v=render(<PaperSimulationPanel state={{results:[],meta:{killSwitchEnabled:false,cycleLimit:3},isLoading:false,error:null,run}}/>);expect(v.textContent).toContain('PAPER ONLY');expect(v.querySelector('button').textContent).toContain('Simulate Approved');expect(run).not.toHaveBeenCalled()})
 it('shows blocked and simulated outcomes',()=>{const v=render(<PaperSimulationPanel state={{results:[],meta:{blocker:'Paper automation kill switch is disabled'},isLoading:false,error:null,run:vi.fn()}}/>);expect(v.textContent).toContain('Blocked:');act(()=>root.unmount());root=createRoot(container);act(()=>root.render(<PaperSimulationPanel state={{results:[{evaluationId:'e',symbol:'AAPL',strategyId:'momentum',status:'SIMULATED_FILLED',orderPlan:{quantity:2,guardrailResult:{approved:true}},simulation:{fillStatus:'filled'}}],meta:{killSwitchEnabled:true},isLoading:false,error:null,run:vi.fn()}}/>));expect(container.textContent).toContain('AAPL · SIMULATED FILLED')})
})
